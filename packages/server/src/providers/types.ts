import type { ModelDescriptor,ProviderCapabilities,ProviderCredential,ProviderGenerateRequest,ProviderResponse,ProviderStreamChunk } from '@espera/shared';
export interface LLMProvider { readonly id:string; readonly name:string; readonly defaultBaseUrl?:string; listModels(credential?:ProviderCredential):Promise<ModelDescriptor[]>; validateCredential(credential:ProviderCredential):Promise<boolean>; generate(request:ProviderGenerateRequest):Promise<ProviderResponse>; stream(request:ProviderGenerateRequest):AsyncIterable<ProviderStreamChunk>; getCapabilities():ProviderCapabilities; }
